doctype html
html(lang="en")
    include head

    body(class='antialiased bg-slate-200 '+bodyClass)

        include nav

        <!-- React Components -->
        section(class="px-6 leading-6 text-gray-700 sm:px-6 lg:px-8")
            div(class="w-full mx-auto duration-500 ease-in-out transform max-w-screen-xl")
                main(id="root")


        <!-- Webpack -->
        script(src='/dist/index.js?v='+version)
        include foot
